import React from "react";
import "./Hero.scss";

const Hero: React.FC = () => {
  return (
    <main className="hero">
      <h2 className="hero__title">Electronics</h2>
    </main>
  );
};

export default Hero;
